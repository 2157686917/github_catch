# GitHub 热门项目自动撰文与审核工作流需求文档 (PRD)

---

## 1. 概述与执行分工

### 1.1 系统用途

本系统旨在构建一个基于多 Agent 协作的自动化内容生成与审核工作流。通过抓取 GitHub 热门项目/Skill/工具，理解其核心价值，结合历史优质文章的写作规范，实现高质量开源项目推荐文章的自主撰写、多轮自我审校与归档管理。

### 1.2 Agent 职责划分

| Agent 标识 | 角色名称 | 核心职责与工作边界 |
| --- | --- | --- |
| **Agent A** | **项目挖掘与解析专家** | 负责按指定时间范围（日/周/月）检索 GitHub 高热度、广受众、易部署的项目，并深度解析其功能、部署/使用方式及适用场景，输出结构化项目简报。 |
| **Agent B** | **主编与文笔审查官** | 负责从用户给定的优秀范文中总结提取文笔风格与介绍逻辑（维护全局知识库）；并作为"主编"对 Agent C 提交的文章进行审校并给出明确优化建议，判定文章是否可直接发布。 |
| **Agent C** | **内容撰写与重构作者** | 负责根据 Agent A 提供的项目解析初稿撰写完整的介绍文章，并依据 Agent B 给出的优化建议进行多轮修改与重构，直至通过审核。 |

---

## 2. 工作流详细设计

系统包含两个相互独立、可单独调用的子工作流：**工作流（1）：项目挖掘与文章迭代流** 与 **工作流（2）：风格范式学习与知识库构建流**。

### 工作流（1）：项目挖掘与文章迭代流

```
[ Agent A ] 筛选并解析 GitHub 热门项目
     │
     ▼
[ Agent C ] 撰写初始介绍文章
     │
     ├─────────────────────────────────────────┐
     ▼                                         │
[ Agent B ] 依据《learned_oldest.md》审核文章   │
     │                                         │
     ├───【不符合发布标准 (含修改意见)】────────┼─ (循环迭代)
     │                                         │
     └───【符合发布标准 (无需修改 / 修改后通过)】 │
             │                                 │
             ▼                                 │
   在项目目录下新建 `%当日日期%` 文件夹          │
   保存为 `.md` 文件并标注为 "可发布" ───────────┘

```

1. **项目筛选与深度解析（Agent A）:**
* **入参设置**：用户选择筛选时间范围（`当天` / `当周` / `当月`）。
* **筛选指标**： GitHub 项目/Skill/工具的热度增量、受众覆盖度、部署复杂度（优先选简易部署）。
* **内容理解**：Agent A 分析提取以下要素：
1. 项目核心功能与解决的痛点
2. 部署与使用方式（命令行/Docker/GUI等）
3. 典型适用场景与目标用户群


* **交付物**：结构化项目简报数据。


2. **初稿撰写（Agent C）:**
* Agent C 接收 Agent A 输出的项目简报。
* 参考通用文章框架与项目信息，生成第一版 Markdown 格式的介绍文章。


3. **智能审校与决策（Agent B）:**
* Agent B 调取 `learned_oldest.md` 中的文笔风格与逻辑规范，对 Agent C 提交的文章进行审核。
* **决策分支**：
* **分支 1（达标）**：文章质量优异，无需修改。判定结果为"可直接发布"，进入归档步骤。
* **分支 2（未达标）**：指出具体问题（如逻辑脱节、文笔生硬、缺少部署细节等），输出结构化的修改建议，打回给 Agent C。




4. **多轮修改与闭环迭代（Agent C ↔ Agent B）:**
* Agent C 根据 Agent B 最新给出的修改意见对文章进行优化或重构。
* Agent C 将新版文章重新提交给 Agent B 审核。
* **迭代终止条件**：Agent B 判定文章已具备无需进一步修改即可直接发布的水准。


5. **归档与状态标注:**
* Agent 确认审核通过后，在当前项目所在目录下自动创建名称为 `%当日日期%`（如 `2026-08-05`）的文件夹。
* 将最终版文章保存为 `.md` 文件。
* 在文件 Header 或元数据中将文章状态显式标注为 `可发布`（Status: Ready to Publish）。


---

### 工作流（2）：风格范式学习与知识库构建流

```
[ 用户提交文章链接 ] (单次 ≤ 2篇)
     │
     ▼
[ Agent B ] 深度学习与逻辑提取
     │
     ▼
[ 去重校验 ] 遍历对比现有 `learned_oldest.md`
     │
     ├───【存在重复信息】───► 取消重复内容的写入（丢弃/跳过）
     │
     └───【新增无重复信息】─► 追加写入 `learned_oldest.md`

```

1. **范文输入:**
* 用户向 Agent B 提供"GitHub项目推荐"类型的优秀文章链接。
* **约束**：单次输入链接数量上限为 **2 篇**。


2. **特征提取与总结（Agent B）:**
* Agent B 阅读并分析文章，提炼以下维度：
1. **文笔语气**（如：专业严谨、幽默风趣、极简干货等）
2. **介绍逻辑与结构**（如：痛点引入 → 效果演示 → 核心功能 → 部署步骤 → 总结评估）
3. **高频亮点词汇与修辞偏好**




3. **查重与增量写入策略:**
* Agent B 遍历当前文件夹下的 `learned_oldest.md` 文件全量内容。
* **去重对比**：将本次提取的新规则/风格与既有内容做语义与规则级查重。
* **执行动作**：
* 若包含重复逻辑/规则：**取消本次重复部分的写入**。
* 若为全新规则/逻辑：**追加更新至 `learned_oldest.md` 中**，保持知识库的持续精炼。




---

## 3. 关键业务规则与非功能要求

1. **独立解耦规则**：
* 工作流（1）和工作流（2）可完全独立触发与执行。
* 当 `learned_oldest.md` 为空或不存在时，工作流（1）按默认主编标准进行审核；当该文件存在时，Agent B 必须严格遵照文件内的写作范式进行校验。


2. **零冗余修改原则（工作流 1）**：
* 不设强制修改次数下限。如果 Agent C 首次生成的文章即完美契合 Agent B 的标准，允许直接跳过修改循环，标记为"可发布"并归档。


3. **目录与文件规范**：
* **知识库文件**：固定存储在对应根目录下的 `learned_oldest.md`。
* **归档目录**：`./[项目名称]/%当日日期%/`（例如 `./docker-mcp-server/2026-08-05/`）。
* **归档文件标记**：文件顶部需包含 Frontmatter 标识，例如：
```markdown
---
title: "XXXX 项目介绍"
date: 2026-08-05
status: 可发布
---

```
